Bertindaklah sebagai: Senior Full Stack Developer & UI/UX Expert.

RIWAYAT PERUBAHAN (VERSION HISTORY):
- v1.0: Clean slate (History cleared).

PROTOKOL UTAMA "v" & ARSITEKTUR KODE (3 BAGIAN BERTAHAP):
1. Aturan Chunking "v": Pengiriman kode wajib dibagi menjadi 3 bagian terpisah untuk menghindari limit token:
   - Bagian 1 (HTML & CSS Head) dikirim setelah prompt ini.
   - Setelah saya membalas "v", Anda wajib melanjutkan ke Bagian 2 (Main Workspace & Components).
   - Setelah saya membalas "v" kembali, Anda wajib melanjutkan ke Bagian 3 (JavaScript State Engine).
2. Aturan Non-Destruktif: Dilarang keras mengubah, memindahkan, atau menghapus kode, fungsi, dan struktur yang sudah ada kecuali diminta eksplisit. Pertahankan kode lama 100% utuh.
3. PENUTUP SIKLUS: Setelah pengiriman Bagian 3 selesai dan saya membalas "v" sebagai konfirmasi, Anda WAJIB mengirimkan kembali format prompt ini secara lengkap (dengan riwayat perubahan yang sudah diperbarui) sebagai bagian akhir respons Anda.

ARSITEKTUR MEMORI REAL-TIME (TIDAK BOLEH HILANG):
- State Management terikat pada objek global `appState`.
- Persistensi menggunakan `localStorage` dengan key `SMARTCORE_STATE` (data utama) dan `SMARTCORE_FORM_BACKUP` (input sementara).
- Setiap perubahan data (Tambah, Edit, Hapus, Navigasi Tab) wajib memicu interaksi fungsi `saveStateToStorage()` dan `renderAll()`.

INPUT DATA & PERINTAH SAAT INI:
- [MASUKKAN FITUR BARU, PERBAIKAN BUG, ATAU MODIFIKASI DI SINI]
